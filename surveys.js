AAApp.context().then(async ctx=>{async function local(){const rows=await ctx.db.getAllSubmissions();document.getElementById('local-summary').textContent=`${rows.length} local`;const t=document.getElementById('local-list');if(!rows.length){t.innerHTML='<div class="empty">No local household surveys yet.</div>';return;}t.innerHTML=rows.map(r=>`<a class="list-row" href="household-survey.html?local_id=${encodeURIComponent(r.local_uuid)}"><span><strong>${ctx.safe(r.household_number||'Household draft')}</strong><small>${ctx.safe([r.barangay,r.zone].filter(Boolean).join(' · ')||'Barangay not entered')} · ${ctx.safe(r.form_status||'draft')}</small></span><span class="record-meta"><i class="status ${ctx.safe(r.sync_status||'pending')}">${ctx.safe(r.sync_status||'pending')}</i><small>${r.has_location?'GPS ✓':'No GPS'}</small></span></a>`).join('');}async function server(){const t=document.getElementById('server-list');if(!ctx.online()||!ctx.client){const cached=await ctx.db.getSetting('cached_server_surveys')||[];render(cached,true);return;}const{data,error}=await ctx.client.from('aa_household_submissions').select('id,household_code,interview_date,barangay,zone,interviewer,status,latitude,longitude,gps_accuracy_m,updated_at').order('updated_at',{ascending:false}).limit(150);if(error){t.innerHTML=`<div class="empty">${ctx.safe(error.message)}</div>`;return;}await ctx.db.setSetting('cached_server_surveys',data||[]);render(data||[],false);}function render(rows,cached){const t=document.getElementById('server-list');if(!rows.length){t.innerHTML='<div class="empty">No synced household surveys yet.</div>';return;}t.innerHTML=(cached?'<div class="cache-note">Offline · showing last synced list</div>':'')+rows.map(r=>`<div class="list-row"><span><strong>${ctx.safe(r.household_code||'Household')}</strong><small>${ctx.safe([r.barangay,r.zone,r.interview_date].filter(Boolean).join(' · '))}</small></span><span class="record-meta"><i class="status synced">${ctx.safe(r.status||'completed')}</i><small>${r.latitude?'GPS ✓':'No GPS'}</small></span></div>`).join('');}document.getElementById('sync-all').addEventListener('click',async()=>{if(!ctx.online()){alert('Offline. Records remain on this device.');return;}const btn=document.getElementById('sync-all');btn.disabled=true;btn.textContent='Syncing…';const r=await AASurveySync.syncAll(ctx);btn.disabled=false;btn.textContent=`Sync done · ${r.ok} uploaded${r.errors?' · '+r.errors+' failed':''}`;await local();await server();});document.getElementById('refresh-server').addEventListener('click',server);await local();await server();}).catch(console.error);